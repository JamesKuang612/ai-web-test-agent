[mcp_servers.playwright]
command = "npx"
args = ["--no-install", "playwright-mcp", "--isolated", "--storage-state=playwright/.auth/jdy.json"]
required = true
default_tools_approval_mode = "approve"
startup_timeout_sec = 30
tool_timeout_sec = 60
